1. Ensure the popup is shown when `FilterEditor` gains focus initially in `NewSessionDialog`. Wait, the user says "if the 'sources' input is focus it should have toggled the dispaly to be open". Actually, `NewSessionDialog` sets focus to `m_filterEditor->lineEdit()` in `showEvent` if there are no selected sources.
2. In `FilterEditor::eventFilter`, for `QEvent::FocusIn` on `m_lineEdit`, the code currently checks `if (m_lineEdit->text().startsWith(QLatin1String("=")))`. If it's a new session, the text might be empty, or it might be prepopulated with a default filter. If it's a default filter that doesn't start with `=`, should the formula builder be shown? Wait, the formula builder is meant specifically for the `=` prefix (advanced queries). Wait! The user says "when I open "New Session" if the "sources" input is focus it should have toggled the dispaly to be open." Meaning, the formula builder popup should be open regardless of the text starting with `=`? No, the text in NewSessionDialog defaults to a formula like `=NOT archived:Yes`.
Wait, if it defaults to `=NOT archived:Yes`, it starts with `=`. So why didn't it show up? Because the `FocusIn` event might happen *before* the text is set, or maybe `showEvent` in NewSessionDialog happens and gives focus, but `isFormula` check passes and it shows.
Wait, let's read `FilterEditor::setFilterText`. If `newText.startsWith("=")`, it shows the popup if `!m_userDismissed`. But the popup might not be shown if the widget is not fully visible yet, so `updatePopupPosition` uses `mapToGlobal` before the dialog is mapped to the screen.
Ah! `mapToGlobal` returns incorrect coordinates (e.g. `0, 0` or similar) if the widget is not yet shown. This explains why the vertical position hasn't updated to match the input!
3. To fix the vertical position, we need to handle the `QEvent::Show` on the `FilterEditor` itself or its `parentWidget()`, and update the position there. Also we should recalculate the position when it's shown.
4. Let's look at `updatePopupPosition`. `m_lineEdit->mapToGlobal(QPoint(0, m_lineEdit->height()))`. If `m_lineEdit` isn't fully laid out, `height()` might be wrong, or `mapToGlobal` might be wrong.
5. In `FilterEditor::eventFilter`, we can catch `QEvent::Show` on `m_lineEdit` or `this` to update the position.
