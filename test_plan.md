Plan:
1. Update `SessionModel` to support `local_notes`. Add `LocalNotesRole` to `SessionModel::SessionRoles` and read/write `local_notes` from `data.rawObject`. Add `setLocalNotes(const QString &id, const QString &notes)` method to update and save the notes.
2. In `SessionWindow`, add a `QTextEdit *m_notesEdit` to display and edit notes. Add it to the tab widget as "Notes". Load the text from `m_sessionData["local_notes"]` and save it when it changes (maybe use `focusOutEvent` or a "Save Notes" button, or emit a signal `notesChanged(id, text)` that `MainWindow` catches to update `m_sessionModel`). Actually, since `SessionWindow` receives a copy of the session data, emitting a signal like `notesChanged(const QString &id, const QString &notes)` is best.
3. In `SessionWindow::renderDetailsAndDiff`, append the first part of the notes to the details HTML.
4. In `MainWindow`, add a "Notes" action to the context menus (like `m_sessionView`, `m_archiveView`, `m_queueView`, etc., or whatever view the user right-clicks on). Clicking it should open a dialog to edit notes or just open the `SessionWindow` directly on the "Notes" tab. The prompt says "choose the 'notes' option in which it has it's own editor window". It might be an input dialog or a small text editor window. Let's create a `NotesDialog` or just use the `SessionWindow` and switch to the notes tab. Actually, "it's own editor window" sounds like a small text dialog that saves notes. Wait, "It should be a tab called "notes", which the first part of it gets displayed in the "Details" but it is in full and editable in it's own "Notes" tab at the end. You should be able to access it from main window by right clicking on an item and choosing the "notes" option in which it has it's own editor window". This could mean the right-click "notes" option opens a window specifically for notes, or it opens the `SessionWindow` and focuses the Notes tab. I will create a `NotesDialog` for the right click or switch to the notes tab if `SessionWindow` is used.
