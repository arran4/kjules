--- CMakeLists.txt
+++ CMakeLists.txt
@@ -7,5 +7,9 @@
-set(QT_MAJOR_VERSION 6)
-set(QT_DEFAULT_MAJOR_VERSION 6)
+if(NOT DEFINED QT_MAJOR_VERSION)
+    set(QT_MAJOR_VERSION 6)
+endif()
+
+set(QT_DEFAULT_MAJOR_VERSION ${QT_MAJOR_VERSION})

 set(CMAKE_AUTOMOC ON)
 set(CMAKE_AUTORCC ON)
@@ -19,5 +23,10 @@
-find_package(Qt6 CONFIG REQUIRED COMPONENTS Core Gui Widgets Network Test DBus)
-find_package(KF6 REQUIRED COMPONENTS CoreAddons I18n Wallet Notifications XmlGui Config WidgetsAddons GlobalAccel Archive Runner)
+if (QT_MAJOR_VERSION STREQUAL "6")
+    find_package(Qt6 CONFIG REQUIRED COMPONENTS Core Gui Widgets Network Test DBus)
+    find_package(KF6 REQUIRED COMPONENTS CoreAddons I18n Wallet Notifications XmlGui Config WidgetsAddons GlobalAccel Archive Runner)
+else()
+    find_package(Qt5 CONFIG REQUIRED COMPONENTS Core Gui Widgets Network Test DBus)
+    find_package(KF5 REQUIRED COMPONENTS CoreAddons I18n Wallet Notifications XmlGui Config WidgetsAddons GlobalAccel Archive)
+endif()

 include(KDEInstallDirs)
@@ -136,3 +145,5 @@
-add_subdirectory(src/runner)
+if (QT_MAJOR_VERSION STREQUAL "6")
+    add_subdirectory(src/runner)
+endif()

 enable_testing()
