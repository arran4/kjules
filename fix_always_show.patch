--- src/filtereditor.cpp
+++ src/filtereditor.cpp
@@ -493,17 +493,17 @@
                         ? text
                         : text + QLatin1Char(' ');
   m_lineEdit->setText(newText);
-  if (newText.startsWith(QLatin1String("="))) {
-    m_toggleButton->setVisible(true);
-    if (!m_userDismissed) {
-      updatePopupPosition();
-      m_popupFrame->show();
-      m_toggleButton->setChecked(true);
-    }
-    updateTreeFromText();
-  } else {
-    m_toggleButton->setVisible(false);
-    m_popupFrame->hide();
+  m_toggleButton->setVisible(true); // Always show toggle, or maybe wait... the user said "toggled when you use the visibility toggle button"
+  // But wait, the user's focus is on the input so it will trigger the focus event anyway.
+  // Let's make it always visible if they want.
+  if (!m_userDismissed) {
+    updatePopupPosition();
+    m_popupFrame->show();
+    m_toggleButton->setChecked(true);
+  }
+  if (newText.startsWith(QLatin1String("="))) {
+    updateTreeFromText();
+  } else {
     m_treeModel->removeRows(0, m_treeModel->rowCount());
   }
   m_updating = false;
@@ -516,17 +516,16 @@
   m_updating = true;
-  bool isFormula = text.startsWith(QLatin1String("="));
-  m_toggleButton->setVisible(isFormula);
-  if (isFormula) {
-    if (!m_userDismissed) {
-      updatePopupPosition();
-      m_popupFrame->show();
-      m_toggleButton->setChecked(true);
-    }
-    updateTreeFromText();
-  } else {
-    m_popupFrame->hide();
+  m_toggleButton->setVisible(true);
+  if (!m_userDismissed) {
+    updatePopupPosition();
+    m_popupFrame->show();
+    m_toggleButton->setChecked(true);
+  }
+  if (text.startsWith(QLatin1String("="))) {
+    updateTreeFromText();
+  } else {
     m_treeModel->removeRows(0, m_treeModel->rowCount());
   }
   m_updating = false;
